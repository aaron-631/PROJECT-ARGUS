"""
Sanitization Layer — strips secrets, control characters, zero-width spaces,
and invisible Unicode before judge insertion to prevent delimiter escape attacks.
"""


def sanitize(raw_output: str) -> str:
    # TODO: Week 7-8
    raise NotImplementedError
